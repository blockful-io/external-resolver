{
  "name": "@blockful/contracts",
  "packageManager": "yarn@4.1.0",
  "scripts": {
    "test": "forge test -vvv",
    "test:watch": "forge test -vvv --watch",
    "start:l1": "anvil & copyfiles ../../.env contracts/.env && forge script script/PublicResolver.s.sol --rpc-url http://localhost:8545 --broadcast -vv",
    "start:db": "anvil & copyfiles ../../.env contracts/.env && forge script script/DatabaseResolver.s.sol --rpc-url http://localhost:8545 --broadcast -vv",
    "start:arb": "anvil & copyfiles ../../.env contracts/.env && forge script script/OffchainResolver.s.sol --rpc-url http://localhost:8545 --broadcast -vv",
    "postinstall": "forge install",
    "lint": "solhint src/**/*.sol --fix",
    "build": "forge build"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "solhint": "^4.1.1"
  }
}