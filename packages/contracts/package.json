{
  "name": "contracts",
  "packageManager": "yarn@4.1.0",
  "scripts": {
    "test": "forge test -vvv",
    "test:watch": "forge test -vvv --watch",
    "start": "anvil & copyfiles ../../.env contracts/.env && forge script script/*.sol --rpc-url http://localhost:8545 --broadcast -vv",
    "postinstall": "forge install"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
